# 快速上架清单

## 先跑通体验版

- 导入微信开发者工具。
- 点击编译，先用演示模式测试页面。
- 开通云开发并替换 `app.js` 中的云环境 ID。
- 部署 `cloudfunctions/optimizeResume`，选择“云端安装依赖”。
- 配置 `DEEPSEEK_API_KEY` 后测试真实 AI 输出。
- 放置中文字体后重新部署云函数，再测试 PDF 生成。

## 第一版建议定价

建议先不要做复杂套餐，直接验证用户是否愿意付小钱：

- 免费：每个 openid 1 次基础评估，展示评分、摘要、欠缺项和部分亮点。
- 3.99 元：解锁当前完整优化文本 + PDF 下载 + 3 次岗位匹配度专业分析额度。

## 支付接入

需要准备：

- 企业或个体工商户主体，并完成微信认证。
- 微信支付商户号，并与当前小程序 AppID 关联。
- 云数据库集合：`users`、`resume_results`、`orders`、`entitlements`。
- 云函数：`optimizeResume`、`getResult`、`getQuota`、`getPdfUrl`、`createOrder`、`payNotify`、`deleteResult`。
- 环境变量：`DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL`、`WECHAT_PAY_SUB_MCH_ID`。
- 正式环境关闭 `ENABLE_MOCK_PAY`。
- 真机测试支付成功、取消支付、重复回调、已解锁再次打开。
- 确认未支付状态下前端拿不到 `optimizedResume` 和 `pdfFileID`。

## 审核材料

建议提前准备：

- 隐私政策：说明会处理用户填写的简历、PDF、手机号或求职信息。
- 用户协议：说明 AI 内容仅供参考，不承诺求职结果。
- 微信公众平台用户隐私保护指引：覆盖简历文本/PDF、姓名、电话、邮箱、教育经历、工作经历、项目经历、openid、订单信息、云存储文件。
- 数据删除说明：结果页提供“删除数据”，可删除云端结果、PDF文件和本地历史。
- 内容安全说明：禁止上传违法、侵权、涉政、涉黄、涉暴内容。
- 客服入口：微信审核常会看服务闭环。

## 获客测试

上线前就可以做流量测试：

- 小红书：发“简历被 HR 秒拒的 5 个原因”。
- 抖音同城：发“应届生简历优化前后对比”。
- 微信群：免费帮前 20 人测评分。
- 朋友圈：用真实前后对比图引导体验。

核心指标：

- 访问到提交简历转化率。
- 提交到支付转化率。
- 单次 AI 成本。
- 每个用户平均收入。
